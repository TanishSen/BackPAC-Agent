# BackPAC agent — Pipecat voice pipeline + LangGraph brain, Claude via Anthropic.
# Same Pipecat line the ajimganj concierge runs, with the extras this agent uses:
#   silero    — voice-activity detection (barge-in)
#   azure     — server-side STT (skip if you only ever run STT_MODE=device)
#   elevenlabs— the voice (eleven_v3 TTS)
#   livekit   — the room transport
pipecat-ai[silero,azure,elevenlabs,livekit]==1.3.0
livekit-api
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
loguru
aiohttp
httpx==0.28.1
langchain-core>=0.3
langchain-anthropic>=0.3
langgraph>=0.2.40
